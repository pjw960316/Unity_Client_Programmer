- Find References
  - script 우 클릭 후 find references 하면 이거 붙은 거 다 찾음
- 코드가 막막할 때?
  - 일단 디버그 포인트 막 찍고 디버그 돌려라
- 디버깅 포인트를 아래에 찍으면 찍을수록 위의 데이터가 다 보이므로 개편함.
  - 근데 사실 아님
  - 예를 들어 디버깅 포인트를 바로 아래로 잡음. 바로 아래로 했으면 previous statueitemcount가 9였을 것.
    - 너무 아래로 잡아버리면 아래 까지 다 연산하고 그 위에 모든 변수를 거기까지 최신화 하므로 바로 아래 잡는 것 보다 부정확하다. 그 사이에 바뀌지 않으면 괜찮지만 
- 디버깅 포인트 함수 진입
  - 함수 진입은 해당 포인트 이전에 찍어준다.
   
- 디버그 할 꺼면 로딩때 디버그 켜놔라

- :star:멤버 변수 줄이는 법
  - 해당 메서드 전체를 람다식 전체로 바꾸고 호출하면 된다.
  - 그럼 그 메서드를 호출하는 메서드에서 또 scope가 생기므로 괜찮다.
- rider
  - 코드 작게 뜨는 곳에서 해당 코드 우클릭 -> perforce -> show history -> compare
  - 에러로 빨리 이동하는 법
    - unity에서 빨간 로그 킨다음에 더블클릭
- 프리팹의 serialized field 값 설정할 때 실수 하는 것
  - 어떤 Timer Script에서 serialized field를 설정했다고 가정한다.
    - 얘는 많은 프리팹에 붙어있다.
  - 그러면 얘가 붙어있는 프리팹 아무거나 가서 serialized field를 변경하고 왜 안되나 하고 있다.
  - 당연히 해당 field 값이 반영되어야 하는 특정 프리팹에 가서 field 값을 설정해야 한다. 
- 람다식은 c# 내부에서 완전히 익명메서드와 동일하게 컴파일 된다.
