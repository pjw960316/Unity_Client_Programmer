# unitask
- async / await
- 기존에 task로 처리하던 것과 동일
- 속도도 빠르고 가독성도 훌륭하다.
- 코루틴 보다 좋은 듯.

# 1.17이어서

# 자식 객체 연결 하는 방식
- 자식 객체를 GetComponent, GetChildren 이런 걸로 찾을 수 있긴 함.
- 근데 문제점이 많음
  - 일단 느림
  - 인덱스나 이름으로 찾으면 이건 하드코딩임.
    - 다른 사람이 해당 순서나 이름을 바꾸면 그냥 박살남.
- serializefield를 추가해서 직접 연결해주자.
  - FormerlySerializedAsAttribute 라는 것도 있다.
  - 해당 이름으로 유지
  - https://docs.unity3d.com/ScriptReference/Serialization.FormerlySerializedAsAttribute.html


# 중요
- 어떤 메서드가 어떻게 쓰였는지 모르겠을 때 항상 그 메서드를 검색하거나 참조된 곳을 찾는다. 그런데 없으면 막막하다. 그럴 때는 해당 메서드를 들고 있는 클래스에 대해서 이해하면 이해하기 더 쉬워진다. 그러므로 해당 클래스에 대해 Shift + f12를 실행해본다.

# 확장성
- 버튼1번에 아래에 텍스트가 생긴다고 가정하자. 그러면 버튼 2번에도 생길 수 있다. 그러면 2번이 생길 때 내 코드가 재사용 될 수 있는지 파악해서 코딩한다.

# 다시 한 번 기억해라, 자식도 serializefield로 연결해주는 것을.. 자식이 여러개고 연속적이라면 이를 배열로 만든 다는 것을...

# 스크립트의 클래스의 필드가 public 이거나 serialize field라면 반드시 어떤 객체에 컴포넌트로 붙어있고 어디선가 여기에 연결 해주는 부분이 있을 것 이다.

# Perforce History와 비교해서 제거하기
- perforce -> compare with latest repository version -> 변경 사항에 대해 관리한다.
  - Accept로 최신 버전으로 덮을 수 있다.
  - 코드 변경 사항 관리가 매우 용이하다.