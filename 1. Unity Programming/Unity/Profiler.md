# 목차
- [목차](#목차)
- [프로파일링을 해야 하는 이유](#프로파일링을-해야-하는-이유)
- [어쩔 수 없는 부하에는 매몰 되지 말아야 한다.](#어쩔-수-없는-부하에는-매몰-되지-말아야-한다)
- [TimeLine View](#timeline-view)

<br/><br/><br/>

# 프로파일링을 해야 하는 이유
- 여기서 부하가 걸릴 것 같다 -> 확실하지 않다.
- 부하가 걸리는 정확한 메서드와 부하의 정도를 알면 효율적으로 부하를 제거 할 수 있다.
  - 부하가 클 것 으로 예측된 코드를 수정했는데 실제로는 미비 했을 수도 있다.
  - 프로그래머의 감 보다는 프로파일링 결과 수치를 믿어야 한다.

- ![Alt text](./Capture/20231013_121019.png)

# 어쩔 수 없는 부하에는 매몰 되지 말아야 한다.
- 모든 자원은 타협이다.
- 어떤 오브젝트 100개를 만들어야 하는데 렉이 걸린다. 이를 최적화 할 수는 있지만 100개를 만드는데 0초가 들게는 불가능하다.
- 스크롤로 100개의 오브젝트를 보여주는 경우 일반 스크롤을 쓰면 100개를 모두 만들고 창을 띄우니까 초기에 렉이 걸리지만 스크롤을 할 때는 부하가 적다. 하지만 OSA를 이용해서 화면에 보이는 1개씩만 생성하면 초기에 바로 뜨지만 스크롤 상황에서 렉이 걸린다. 그러므로 적절한 방식을 채택해야 한다.
- 오브젝트 풀링도 마찬가지다.
  - 이 컨텐츠를 위해 풀링하는 게 게임 로드 전에 미리 메모리를 쓸 가치가 있는가? 게임 메모리 부족 이슈도 있는데? 그런 것도 생각해야 한다. 다 풀링해 버리면 문제 잖아.
  
<br/><br/><br/>

# TimeLine View
- ![Alt text](./Capture/20231013_160558.png)
- 계층 구조다.
  - 맨 위에 있는 블록을 1단계로 쪼개면 그 아래 블록이고, 그 블록들을 또 쪼개면 아래 블록들로 이루어진다.
  - 예를 들어 200ms가 걸리는 어떤 함수를 발견했다. 그 함수는 2가지 기능으로 이루어져서 한 기능이 120ms, 다른 하나가 80ms를 동작시키면 200ms 함수 아래에 2개의 블록이 각각 120ms와 80ms로 존재할 것 이다.
- 파란색 블록은 스크립트를 의미하고 7호선 색 블록은 엔진 코드를 의미하므로 실질적으로 눈여겨 볼 항목은 파란색 블록이다.
  - 파란색 블록이 스크립트를 담당하므로 내가 작성한 스크립트와 메서드 이름이 블록위에 적혀있다.