## :fire: for문에서 중간에 원소를 삭제하는 경우 <br> index 와 container의 크기 변경에 주의한다.
- for문의 idx++은 최종 판별식이니, 다시 0부터 돌리고 싶으면 idx = -1이 필요하다.
- for문에 list.Count를 넣어서 매 번 갱신 시키므로 값 타입 캐싱한다.

<br><br>

## :fire: 많이 반복되는 for문에서, 방향과 등호는 통일 시켜야 실수 하지 않는다.