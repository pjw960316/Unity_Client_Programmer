# 목차
- [목차](#목차)
- [람다식](#람다식)
- [람다식의 생성](#람다식의-생성)
- [클래스의 멤버 변수를 줄일 수 있다.](#클래스의-멤버-변수를-줄일-수-있다)
- [언제 쓸까](#언제-쓸까)

# 람다식
- 이벤트 지향 프로그래밍에 적합하다.
  - 콜백 메서드를 쉽게 만들 수 있기 때문이라 생각한다.
- 메서드를 간편하게 즉흥적으로 구현해서 사용하는 것이 목적이다.
- return 도 생략이 가능하지만 가독성 측면에서는 달아주자.
- 매개변수가 1개일 때 괄호는 선택사항. 2개부터는 명시한다. 매개변수의 타입을 쓰지 않아도 컴파일러가 유추해 주지만 명시해도 괜찮다.
  - ![20221229_165215](https://user-images.githubusercontent.com/55792986/209920578-49a1ccb5-56e4-4f5c-9fd5-fbcaf0cb6a0d.png)
- 컴파일러는 람다식을 보고 Func, Action, Delegate 일 것 으로 유추한다.
~~~c#
object parse = (string s) => int.Parse(s);   // Func<string, int>
Delegate parse = (string s) => int.Parse(s); // Func<string, int>
~~~

# 람다식의 생성
- 델리게이트 -> 무명메서드(=익명메서드 =annonymous method) -> 람다식
~~~c#
private delegate void MyDelegate(int _value); //델리게이트형 선언
MyDelegate value = delegate(int _value) //무명메서드 정의
{
	Console.Write(_value);
};

value(100); //델리게이트 사용
~~~
- 델리게이트에 넣어 줄 메서드에 대해서 이름이 만들기 귀찮으니 그냥 delegate를 붙여서 만든다.
  - 여기서 만든 것이 무명메서드다.
    - 무명메서드는 한 번만 사용 된다.
  - 이게 또 귀찮아서 람다식으로 줄였다.
  - 람다식은 C# 내부에서 무명메서드와 완전히 동일하게 컴파일 된다.
- ![image](https://user-images.githubusercontent.com/55792986/209919687-b8305537-4f60-48f0-a91f-fc758f0a6e82.png)
  
# 클래스의 멤버 변수를 줄일 수 있다.
- 지금까지 나는 코딩테스트에서 전역변수를 쓰듯, 클래스에서 멤버 변수를 남용했다. 
  - 멤버 변수를 사용하면 클래스 내부 메서드 어디에서든 쉽게 참조할 수 있기 때문이다.
- 하지만 클래스의 멤버 변수는 scope가 넓다. 
  - scope가 넓으면 어디서든 쉽게 참조하고 변경할 수 있기 때문에 위험하다.
- :star:**람다식을 만들면 body 부분이 새로운 scope로 잡힌다. 해당 람다식 밖에서는 람다식보다 넓은 범위의 scope가 잡히므로 클래스의 멤버 변수보다는 scope가 좁고, 람다식 보다는 넓은 변수를 생성할 수 있다.**

# 언제 쓸까
- 가독성의 측면에서 바라 보면 언제 람다를 쓸 지 메서드로 만들 지를 고민하는 순간이 많이 온다.
- 익명 함수 및 표현식은 전체 메서드를 만드는 데 필요한 추가 작업의 이점이 없는 일회성 메서드에 유용하다.
- BingGPT
  - 그러나 이러한 변경이 항상 바람직한 것은 아닙니다. 람다 표현식은 간결하고 편리하지만, 복잡한 로직이나 여러 줄의 코드에는 적합하지 않을 수 있습니다. 따라서 코드의 복잡성과 가독성을 고려하여 결정해야 합니다. 또한, 디버깅이 더 어려울 수 있으므로 이 점도 고려해야 합니다.
  - 마지막으로, 이 변경이 프로젝트의 코딩 스타일 가이드라인과 일치하는지 확인하는 것이 중요합니다. 일관된 코딩 스타일을 유지하는 것은 코드의 가독성과 유지 관리성을 향상시키는 데 중요합니다.

