## :fireworks: 리팩터링 과정에서 중복을 줄인 기법을 정리해보자. <br> 언젠가는 리팩터링 없이 바로 구현 할 수 있도록.

<br><br>

## :one::fire: 일단 구현하고, 중복 되는 부분만 찾아서 메서드로 만들어 준다. <br> 이 때 switch-case를 사용하는 것을 두려워하지 말자. <br> :fire: switch-case가 메서드 중간에 섞여 있으면 좋지 않지만, switch-case만 존재하는 메서드를 사용하는 것은 나쁘다고 생각하지 않는다. <br> switch-case가 나쁜 게 아니라, switch-case가 다른 것과 섞여 있기 때문에 나쁜 코드가 되는 것 이다.
~~~c#
public Vector3 GetPosition(EPath path, int offset)
{
    return path switch
    {
        EPath.Bottom => new Vector3(_environment_X_Length * offset, 0, 0),
        EPath.Top => new Vector3(_environment_X_Length * offset, 0, _environment_X_Length * _oneSideEnvironmentCount),
        EPath.Left => new Vector3(0, 0, _environment_Z_Length * offset),
        EPath.Right => new Vector3(_environment_Z_Length * _oneSideEnvironmentCount, 0,
            _environment_Z_Length * offset),
        _ => throw new NullReferenceException()
    };
}
~~~

<br><br>

## :two::fire: DTO를 struct 또는 class로 class 외부에 적극적으로 만들어준다. <br> :fire: 단, DTO의 Naming에 신경쓰자.